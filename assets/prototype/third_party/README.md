# Third-Party Prototype Assets

This directory is reserved for temporary prototype-only external assets.

Current imports:

- none

When adding external material:

1. create a source directory such as `isometric_city/`
2. add `NOTICE.md`
3. list the imported files and upstream source
4. preserve upstream license text if applicable
5. mark the assets for replacement before commercial release
